name: med
description:

publish_to:

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: 6.1.4
  awesome_notifications: ^0.10.0
  timezone: ^0.10.0
  uuid: ^4.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.0.4
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  flutter_launcher_icons: ^0.9.2
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - lib/l10n/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"
  android_icon_size: 200 # Размер иконки для Android
  ios_icon_size: 200 # Размер иконки для iOS

